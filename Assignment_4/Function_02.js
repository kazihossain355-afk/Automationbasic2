// Assignment:
// Create a function named multiplyNumbers that takes two numbers as parameters and returns their multiplication.

function multiplyNumbers(num1, num2) {
    return num1 * num2;
}

let result = multiplyNumbers(3, 4);

console.log(result);