// Assignment:
// Create a function named sumAllNumbers that uses the rest operator (...) to accept multiple numbers and returns them as an array.

function sumAllNumbers(...numbers) {
    return numbers;
}

console.log(sumAllNumbers(1, 2, 3, 4, 5));