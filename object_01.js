const user ={
            useName: "kazi",
            age: 30,
            gender: "Male"


             }
function userFunction(anyObject) {  
    console.log(`username is ${anyObject.useName} and age is ${anyObject.age}`); 
    
    console.log("username is ${anyObject.useName} and age is ${anyObject.age}"); 

}   console.log(username is ${anyObject.useName} and age is ${anyObject.age}"); 


userFunction(user);          